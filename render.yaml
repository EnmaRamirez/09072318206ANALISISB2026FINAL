services:
  - type: web
    name: netguardgt-api
    env: dotnet
    plan: free
    buildCommand: dotnet publish -c Release -o publish
    startCommand: dotnet publish/NetGuardGT.Api.dll
    autoDeploy: true
